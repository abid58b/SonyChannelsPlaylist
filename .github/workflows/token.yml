name: Update Token JSON Files

on:
  schedule:
    - cron: '*/10 * * * *'
  workflow_dispatch:

jobs:
  update-data:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Code ko checkout karen
        uses: actions/checkout@v4

      - name: 2. Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20' # Recommended latest stable version

      - name: 3. Install dependencies
        run: echo "No external dependencies to install."
      
      - name: Fetch JS Script 
        run: |
          curl -L -o sonyliv-token.js ${{ secrets.SONY_TOKEN_SCRIPT }}
          node sonyliv-token.js

      - name: Changes ko commit karen
        run: |
          git config --global user.name 'github-actions[bot]'
          git config --global user.email 'github-actions[bot]@users.noreply.github.com'
          git remote set-url origin https://${{ secrets.GITHUB_TOKEN }}@github.com/abid58b/SonyChannelsPlaylist.git

      - name: Commit and push changes
        run: |
          git add sony-token.json
          git fetch origin main
          git reset --soft origin/main
          if git diff --cached --quiet; then
            echo "No token changes to commit"
          else
            git commit -m "Fetch json token latest data"
            git push origin main
          fi
